function repeatStringNumTimes(str, num) {
  // repeat after me
  if (num >= 0) {
  let arr = str.split();
  for (let i=1; i<num;i++) {
    arr.push(str);
  }
  str = arr.join('');
  return str;
  }
  else{
return "";
  }
}

console.log(repeatStringNumTimes("abc", 3));
